import styled from 'styled-components';
import { Spin } from 'antd';

const StyledSpiner = styled(Spin)`
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 60vh;
`;

export default StyledSpiner;
